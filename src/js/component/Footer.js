import React from "react";
import ReactDOM from "react-dom";
import PropTypes from "prop-types";

export const Footer = () => {
	return (
		<div className="card text-center sticky-footer">
			<h5 className="card-title">Footer with copyrights</h5>
		</div>
	);
};
